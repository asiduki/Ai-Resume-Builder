const express = require("express");
const ResumeSchema = require("../Models/ResumeData");
const bcrypt = require('bcrypt');

export const resumedataouput = async(req,res)=>{
    try{
        const {project, skills, personalInfo , Experience , education} = req.body ;
        const finduser = 
    }
} 

